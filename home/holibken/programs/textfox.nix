{ ... }:

{
  textfox = {
    enable = true;
    profiles = [ "default" ];
    config = {
      displayNavButtons = true;
      displayWindowControls = false;
      tabs.horizontal.enable = true;
    };
  };
}
